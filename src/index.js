// import `.scss` files
import './scss/styles.scss';
// import Accordion class
import { Accordion as defaultExport } from './lib/accordion';
// export default Accordion class
// I used `defaultExport` to state that variable name doesn't matter
export default defaultExport;
